# 2026-08-04 工作日志

## 微信生态周报 p14 发布（2026.7.27–8.2）

- **状态**：✅ 内容 + 构建 + 全项校验 + 提交 + 推送 + 线上验证 全部完成
- **commit**：`f2729af`（c262cd2..f2729af），线上 https://frost-cao.github.io/wechat-weekly/ 已确认 active=p14、17 期、footer 8/4→8/11
- **方法**：沿用 `add_p9.py` 模式新建 `add_p14.py`（Python 三引号保留 `${ICONS.xx}`），一次脚本完成 5 处改动：PERIODS 头加 p14、activePeriodId='p14'、p13 去 active、footer 改期、插入 p14 块（锚点用 p13 的 `<!-- ════ 期次13：... ════ -->` 注释行，比 div 标签更稳）。改父目录构建源后 `cp` 同步仓库镜像。
- **本期**：8 维度 19 条。视频号 2 / 微信小店 6 / 公众号 2 / 微信支付 1 / 微信客户端 3 / 企业微信 1 / 开放平台·小程序 2 / 推客 2。
- **重点**：① 视频号「短视频带货场景 AI 应用」治理公告（8/10 起带货短视频含 AI 内容须主动标注，否则限制分发）；② 小店新规速递-20260727 + 功能调整汇总-20260731（5 个 API）+ 交易纠纷/文玩文创规则 8/3 生效 + 医疗器械类目新规征集；③ 公众号 AI「一键排版」上线（7/31）；④ 微信支付 AI 接入工具箱（7/28）；⑤ 企微「大圆」内测。
- **链接合规**：9 个 link-btn 全部 store.weixin.qq.com 官方域名；其余 10 条无官方文档页的条目均标注「媒体汇总/非官方公告」灰字。
- **校验全绿**：17 期块、active 唯一=p14、script=1、printPDF=1、mobile=1、footer=1、noResult=1、div 全局平衡 1533/1533、JS `new Function()` 可解析、8 个维度 dim-count 与实际条目数逐一核对一致。

### 经验补充
- 逐期 div 平衡脚本若从 `id="pN">` 开始切片，会漏掉开头的 `<div`，每期必然显示 `N/N+1`。这是**正常现象**，判断标准应是「所有期次呈现同一偏移量」+「全局开合相等」，不要误判为结构损坏。
- 本环境 bash **无 `sleep` 命令**，也没有 `gh` CLI。等待部署改用 `node -e "setTimeout(()=>{},90000)"`，验证线上直接 curl 加随机查询串绕缓存。
