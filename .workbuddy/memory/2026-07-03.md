# 2026-07-03 工作日志

## 修复：index.html / build_html.js 缺失 `<script>` 开标签（第3次修复）
- 用户反馈"又出问题了"。curl 抓取线上确认结构正常（12 期、p9 active、字节数对），但页面 JS 全部不工作。
- 根因：`index.html` 和构建源 `build_html.js` 都**只有 `</script>` 闭标签、缺 `<script>` 开标签**（此前反复修 p8 div、re-sync 时把开标签那行弄丢了）。浏览器把整段 JS 当纯文本，Tab 切换 / 截止日提醒 / 分享按钮全部失效。
- 排查技巧：`grep -c '<script'` 对 `</script>` 匹配不到（`<` 后面是 `/` 不是 `s`），故返回 0；需读到 JS 内容起止行确认。
- 修复：在 build_html.js 中 `const PERIODS` 前补 `<script>`；cp 同步到 wechat-weekly 副本；父目录 `node build_html.js` 重建 → wechat-weekly 跑 `node ../clean_tags.js`。
- 校验：重建后 index.html = 495,062 字节（> 已提交 494,522，内容未丢），各期 item 数与修复前一致，JS 语法可解析。
- 提交 `790a3a1`（index.html + build_html.js），工作流 success，线上已更正。
- 已把"`<script>` 开标签存在性检查"加入 MEMORY.md 的发布前检查清单第 7 项。
